/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com', 'placeholder-image-service.onrender.com'],
    },
}

module.exports = nextConfig